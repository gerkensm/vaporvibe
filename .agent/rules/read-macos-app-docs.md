---
trigger: glob
globs: scripts/macos-app/**/*
---

When working on files in `scripts/macos-app/`, you MUST read the documentation at `docs/NOTARIZATION.md` and `docs/macos-app.md` to understand the macOS app packaging and notarization process.
